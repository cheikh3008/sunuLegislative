{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

<div class="container">
    {% for message in app.flashes('error') %}
    <div class="text-center alert alert-danger mt-2" role="alert">
        {{ message }}
    </div>
    {% endfor %}
    <div class="content">
        <div class="card">
            <div class="card-header">
                <h4>Ajouter un nouveau représentant</h4>
            </div>
            <div class="card-body">
                {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}